Index: source/cppumaker/cpputype.cxx
===================================================================
RCS file: /cvs/udk/codemaker/source/cppumaker/cpputype.cxx,v
retrieving revision 1.43
diff -u -r1.43 cpputype.cxx
--- source/cppumaker/cpputype.cxx	12 Oct 2006 11:33:25 -0000	1.43
+++ source/cppumaker/cpputype.cxx	7 Jan 2007 19:17:35 -0000
@@ -1279,6 +1279,10 @@
           << scopedCppName(rtl::OUStringToOString(
                             m_reader.getSuperTypeName(i), RTL_TEXTENCODING_UTF8));
     }
+#if defined MACOSX && __GNUC__ > 3
+	if (!m_reader.getSuperTypeCount())
+        o << " : public " << "::sal::MacOSXGCC4BaseClass";
+#endif  // MACOSX && __GNUC__ > 3
 
 	o << "\n{\npublic:\n";
 
@@ -2372,6 +2376,11 @@
     if (base.getLength() > 0) {
         o << ": public " << scopedCppName(base);
     }
+#if defined MACOSX && __GNUC__ > 3
+	else {
+        o << " : public " << "::sal::MacOSXGCC4BaseClass";
+    }
+#endif  // MACOSX && __GNUC__ > 3
     o << " {\n";
     inc();
     o << indent() << "inline " << m_name << "() SAL_THROW(());\n";
@@ -3069,6 +3078,10 @@
     }
 	if (superType.getLength() > 0)
 					o << " : public " << scopedCppName(superType);
+#if defined MACOSX && __GNUC__ > 3
+	else
+					o << " : public " << "::sal::MacOSXGCC4BaseClass";
+#endif  // MACOSX && __GNUC__ > 3
 		
 	o << "\n{\npublic:\n";
 	inc();
