Index: bf_forms/util/makefile.mk
===================================================================
RCS file: /cvs/framework/binfilter/bf_forms/util/makefile.mk,v
retrieving revision 1.5
diff -u -r1.5 makefile.mk
--- bf_forms/util/makefile.mk	7 Sep 2005 15:57:20 -0000	1.5
+++ bf_forms/util/makefile.mk	22 Dec 2005 20:07:46 -0000
@@ -100,19 +100,10 @@
 RESLIB1SRSFILES=$(RES1FILELIST)
 
 .IF "$(GUI)"=="UNX"
-
-# [ed] 6/19/02 Only add in libraries for X11 OS X builds
-
-.IF "$(OS)"=="MACOSX"
-.IF "$(GUIBASE)"=="unx"
-SHL1STDLIBS +=\
-        -lX11 -lXt -lXmu
-.ENDIF
-.ELSE
+.IF "$(OS)"!="MACOSX"
 SHL1STDLIBS +=\
 	-lX11
-.ENDIF # OS == MACOSX
-
+.ENDIF
 .ENDIF
 
 # --- Targets ----------------------------------
