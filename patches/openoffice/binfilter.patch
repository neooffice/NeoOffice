Index: bf_forms/util/makefile.mk
===================================================================
RCS file: /cvs/framework/binfilter/bf_forms/util/makefile.mk,v
retrieving revision 1.6
diff -u -r1.6 makefile.mk
--- bf_forms/util/makefile.mk	7 Apr 2006 13:15:37 -0000	1.6
+++ bf_forms/util/makefile.mk	30 Jun 2006 19:00:05 -0000
@@ -101,19 +101,10 @@
 RESLIB1SRSFILES=$(RES1FILELIST)
 
 .IF "$(GUI)"=="UNX"
-
-# [ed] 6/19/02 Only add in libraries for X11 OS X builds
-
-.IF "$(OS)"=="MACOSX"
-.IF "$(GUIBASE)"=="unx"
-SHL1STDLIBS +=\
-        -lX11 -lXt -lXmu
-.ENDIF
-.ELSE
+.IF "$(OS)"!="MACOSX"
 SHL1STDLIBS +=\
 	-lX11
-.ENDIF # OS == MACOSX
-
+.ENDIF
 .ENDIF
 
 # --- Targets ----------------------------------
